# Environment files (never commit real secrets)
.env
.env.local
.env.*.local
**/.env
**/.env.local

# Dependencies & build output
node_modules/
dist/
.next/
trust-policy.json

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# Terraform provider dirs (belt-and-suspenders)
.terraform/
**/.terraform/
